[Unit]
Description=Reboot the machine (scheduled maintenance restart)
# Only meant to be triggered by daily-reboot.timer — see doc/pm2.md
# "Scheduled Daily Reboot" for why this exists (no ECC RAM on the Pi).

[Service]
Type=oneshot
ExecStart=/sbin/reboot

