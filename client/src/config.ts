/** Minimum time (ms) to show the loading state before resolving. */
export const ACTION_MIN_MS = 250;

/** Time (ms) to show the success/error state before resetting to idle. */
export const ACTION_DONE_MS = 700;

/** Time (ms) the fullscreen black screen keeps the display awake before allowing it to turn off normally (the overlay itself stays open). Default: 120 minutes. */
export const BLACK_SCREEN_KEEP_AWAKE_MS = 120 * 60 * 1000;

